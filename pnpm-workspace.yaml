packages:
  - spacetimedb
  - apps/*
  - packages/*

# Native build scripts these packages need to run on install.
allowBuilds:
  esbuild: true
  lightningcss: true
  workerd: true # wrangler's local runtime; downloads a platform binary on install

minimumReleaseAgeExclude:
  - spacetimedb@2.10.0

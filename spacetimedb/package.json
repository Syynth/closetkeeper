{
	"name": "@closetkeeper/module",
	"version": "0.0.0",
	"private": true,
	"description": "SpacetimeDB module for Closetkeeper",
	"type": "module",
	"scripts": {
		"build": "spacetime build",
		"publish:dev": "spacetime publish closetkeeper-dev --server maincloud",
		"typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.test.json"
	},
	"license": "Apache-2.0",
	"dependencies": {
		"spacetimedb": "2.10.*"
	},
	"devDependencies": {
		"@types/node": "^26.4.1",
		"typescript": "^7.0.2"
	}
}
